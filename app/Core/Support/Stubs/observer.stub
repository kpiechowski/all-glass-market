<?php

declare(strict_types=1);

namespace {{RootNamespace}}\{{ModuleName}}\Infrastructure\Persistence\Observers;

use {{RootNamespace}}\{{ModuleName}}\Domain\Models\{{SingularName}};

/**
 * Adapter between the ORM lifecycle and the domain. Keep it thin: dispatch a
 * domain event and let a listener do the work. Never resolve a service here —
 * that welds business logic to save() where nothing can test it.
 *
 * Other hooks are available (creating, updating, saving, restored,
 * forceDeleted…); add only the ones this model actually needs.
 */
class {{SingularName}}Observer
{
    public function created({{SingularName}} $model): void
    {
        //
    }

    public function updated({{SingularName}} $model): void
    {
        //
    }

    public function deleted({{SingularName}} $model): void
    {
        //
    }
}
